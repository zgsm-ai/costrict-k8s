Costrict SSH Manager Service deployed successfully!

Namespace: {{ .Release.Namespace }}
Release: {{ .Release.Name }}
Application: {{ .Values.appName }}

Service:
  Type: {{ .Values.service.type }}
  Port: {{ .Values.service.port }}
  {{- if eq .Values.service.type "NodePort" }}
  NodePort: {{ .Values.service.nodePort }}
  Access URL: ssh://<NODE-IP>:{{ .Values.service.nodePort }}
  {{- else }}
  Cluster IP: {{ .Values.service.name }}.{{ .Release.Namespace }}.svc.cluster.local
  {{- end }}

Image: {{ .Values.image.repository }}:{{ .Values.image.tag }}

SSH Access:
  {{- if .Values.secret.enabled }}
  Password is stored in secret: {{ .Values.secret.name }}
  {{- else if .Values.ssh.rootPassword }}
  Password is configured in values.yaml
  {{- else }}
  No password configured, using default
  {{- end }}

Check the pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app={{ .Values.appName }}

View logs:
  kubectl logs -n {{ .Release.Namespace }} -l app={{ .Values.appName }}

To connect to the SSH service:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ .Values.service.name }} <LOCAL-PORT>:{{ .Values.service.port }}
  ssh root@localhost -p <LOCAL-PORT>